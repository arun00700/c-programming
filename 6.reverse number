// Online C compiler to run C program online
#include<stdio.h>
int main()
{ 

  int no, digit, sum = 0;

    printf ("Insert a number \n"); 
    scanf ("%d", &no); 
    while  (no > 0) 
    { 
        digit = no % 10; 
        printf("%d\n",digit );
        sum = sum * 10 +digit;//sum=0*10+6 =6 ->sum=6*10+5 =65 ->sum=65*10+4=654
        printf("%d\n",sum);
        no /= 10;
        printf("%d\n",no);
    } 
    
    printf("Sum of the numbers %d ", sum); 
  return 0;
} 
